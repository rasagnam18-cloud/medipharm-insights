const menu=document.querySelector('.menu');const links=document.querySelector('.nav-links');menu?.addEventListener('click',()=>{const open=links.style.display==='flex';links.style.display=open?'none':'flex';links.style.position='absolute';links.style.top='86px';links.style.left='0';links.style.right='0';links.style.padding='22px';links.style.background='rgba(245,246,244,.98)';links.style.flexDirection='column';links.style.borderBottom='1px solid #d8e4df'});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>{if(innerWidth<=800)links.style.display='none'}));
const form=document.querySelector('#contactForm');form?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('Project enquiry — Medipharm Research Pvt Ltd');const body=encodeURIComponent(`Name: ${data.get('name')}\nCompany: ${data.get('company')||'Not provided'}\nEmail: ${data.get('email')}\nProject type: ${data.get('project')||'Not provided'}\n\nProject / research need:\n${data.get('message')}`);const status=document.querySelector('.form-status');if(status)status.textContent='Opening your email client…';window.location.href=`mailto:hello@medipharminsights.com?subject=${subject}&body=${body}`});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.section,.stats,.card,.steps>div,.expertise-list>div').forEach(el=>{el.classList.add('scroll-reveal');observer.observe(el)});